const hostForm = document.querySelector('#hostForm');

async function loadChatterbox(hostname) {}

window.onload = function () {};

hostForm.addEventListener('submit', async (e) => {
    e.preventDefault();
});
